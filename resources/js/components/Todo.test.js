import React  from 'react';
import {render, getByLabelText, fireEvent }  from '@testing-library/react'
import Todo  from './Todo'

test('admin root', () => {
		expect(deleteFn(toinput === 'admin').toBe('adminroot'))
})